import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center items-center h-screen'>
            <button className="btn loading text-orange-500 bg-red-100">loading</button>
        </div>
    );
};

export default Loading;